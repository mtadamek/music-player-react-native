import React, { Component } from "react";
import { Text, View } from "react-native";

class Settings extends Component {
  render() {
    return (
      <View
        style={{
          flex: 1,
          justifyContent: "center",
          alignItems: "center",
          backgroundColor: "#373737"
        }}
      >
        <Text style={{ color: "#ececec" }}>Soon...</Text>
      </View>
    );
  }
}

export default Settings;
